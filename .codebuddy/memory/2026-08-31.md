# 2026-08-31

## 按钮状态 + 断网/验证码 综合测试脚本（已完成）
- 新增 `_test_button_states_full.py`（14 用例，全通过），运行：`python-3.13.14-embed-amd64\python.exe _test_button_states_full.py`
  - 套件1 按钮状态矩阵 A1-A9：step0 角色页(prev/next 禁用、开始传输隐藏、EULA 控制角色按钮)、step1(发送端导出配置可见/接收端隐藏、prev/next 启用)、step2(开始传输显示启用、next 禁用)、step3(发送端验证码横幅、传输中 next 禁用)、传输完成(发送端 next 禁用/接收端"查看总结 >")、step4 总结页(next="完成"、查看校验报告禁用、完成并退出)、step5 完成页(prev 禁用、next="完成"、完成并关闭)、传输中开始传输禁用、回退 step2 按钮"重新接收"
  - 套件2 断网 B1-B2：接收端 `_network_down` 分支（提示"网络连接已中断"、按钮"重新接收"、不发 done、不进配置导入）、发送端空闲超时（提示连接中断、停服务器、按钮"重新启动传输"、不进完成页）
  - 套件3 验证码 C1-C3：接收端 files==0（"< 返回修改验证码"、"重试接收"、不发 done）、发送端 auth_failed（提示"验证码有误"、保持服务器、修正后恢复）、HTTP 层（无/错验证码 403 + auth_failed_flag、正确 200 清除）
  - 脚本末尾打印人工测试步骤总结 S1-S14

## 顺带修复两条过时测试断言（均非产品 bug）
1. `_test_ui_full.py` 用例 41：`show_transfer_error` 状态标签文案在 0c7c3da（8-28 断线修复提交）起已从"网络连接已中断"改为"请返回修改验证码"（避免验证码错误时误导为断网），断言未同步 → 更新为断言"请返回修改验证码"。
2. `_test_user_cases.py` TC-06b/06c：`_resolve_conflicts` 在 8-28 线程安全整改后经 `_post_ui` 入 `_ui_q` 队列（不再 `after(0,...)`），测试仍用 `pump_after` 驱动弹窗导致按钮从未创建 → 在 `pump_after` 后补充 `t.ctl._poll_ui_q()` 手动 drain 队列。
- 同时更新 `_source_mark_auth_failed` 中过时注释（"写为网络连接已中断"→"写为请返回修改验证码"）。

## 回归确认（全部通过）
ui 60/60、func 30/30、disconnect 7/7、buttons 14/14、readability 11/11、usercases 21/21、replacement 25/25。

## 约定
- 嵌入式 Python：`python-3.13.14-embed-amd64\python.exe`。
- 修改控件文案类 UI 行为时，需同步检查 `_test_ui_full.py` / `_test_user_cases.py` 中的文字断言（历史多次因文案不同步导致测试过时）。
- 后台线程 UI 一律走 `_post_ui` 队列；无 mainloop 的测试若要驱动对话框等队列任务，需手动 `ctl._poll_ui_q()`。
