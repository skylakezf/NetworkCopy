## 2026-06-16

### 组件本地化部署
- 创建 `setup_tkinter.py`: 从本地 `python_31314.nupkg` 提取 tkinter 组件
  - 提取 tcl86t.dll / tk86t.dll
  - 提取 _tkinter.pyd
  - 提取 tkinter/ Python 包
  - 提取 tcl/ Tcl 脚本
- 创建 `setup.bat`: 一键运行 tkinter 部署
- 更新 `run.bat`: 启动时自动检测 `_tkinter.pyd` 是否存在，不存在则自动调用 setup_tkinter.py
- 删除多余文件: `extract_tk.py`, `extract_tk.bat`
- 程序所需组件：
  - 运行时: python-3.13.14-embed-amd64/ (嵌入式 Python + 标准库)
  - tkinter: 从本地 nuget 包提取（无需联网）
  - 系统 DLL: kernel32.dll, iphlpapi.dll, setupapi.dll (Windows PE 自带)
  - 所有 Python 依赖均为标准库，无需 pip
