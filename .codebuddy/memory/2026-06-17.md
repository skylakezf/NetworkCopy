## 2026-06-17

### tkinter 组件本地化 - 已解决
- 从 `F:\Python3.13.14` 复制 tkinter 文件到嵌入式 Python
- DLL 放 `DLLs/` + `._pth` 加 `.\DLLs`
- 必需: `_tkinter.pyd`, `tcl86t.dll`, `tk86t.dll`, `zlib1.dll`(关键!), `vcruntime140.dll`, `vcruntime140_1.dll`, `tcl/`, `tkinter/`

### 网络架构 - 目标端 DHCP, 源端 HTTP 服务器
xi
  - 源: `_setup_source_network()` 跑 ipconfig /renew 从目标DHCP获取IP
  - 目标: `_setup_target_dhcp()` 设169.254.100.1→启动DHCP→60s自动选单
- 目标下"发现设备"下拉框显示 DHCP 客户端 (IP|MAC|Hostname)
- APIPA 扫描为回退

### 文件传输过滤
- `file_transfer.py` `_handle_list()`: 跳过 `AppData\`、`$` 前缀文件夹、`System Volume Information`
- `do_GET` 对查询参数加 `urllib.parse.unquote()` URL 解码修复中文文件名 404

### 磁盘名称获取修复
- UI 默认值改为 `("请先选择设备类型")` / `("扫描中...")`
- IOCTL 提升为主力，新增注册表回退
