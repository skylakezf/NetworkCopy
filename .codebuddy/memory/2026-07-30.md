# 2026-07-30

## 代码恢复 (Git)
- 从 Git HEAD (66d0e53) 恢复了所有 10 个 Python 源文件
- 从 Git 恢复了 build.bat, run.bat, .spec 等配置

## 编译环境修复
- `build.ps1`: 移除 ttkbootstrap 引用 (当前 HEAD 代码用纯 tkinter)
- 嵌入式 Python (python-3.13.14-embed-amd64) 环境完整: PyInstaller 6.21.0, cryptography 49.0.0, tkinter

## 聊天历史复现 (来源: history_202607302240.md, 截止 21:00)

### UI 重构: 横板向导式布局 (ui.py 重写)
- 16:9 横板布局: 左侧内容区 + 右侧步骤指示器
- 5 步向导流程: 选择角色 → 选择网卡 → 选择磁盘 → 连接设置 → 传输状态
- 角色选择: 旧电脑(送文件)/新电脑(收文件) 卡片式选择 (悬停高亮)
- **改用 ttkbootstrap 2.0.1 (flatly 主题)** — `ttkbootstrap.Window` 替代 `Tk`
- 使用 `_tk.Frame` 和 `_tk.Label` 处理需要 `bd`/`relief`/`foreground`/`background`/`bg` 的控件
- ttkbootstrap 控件使用 `bootstyle` 参数实现主题化 (如 `bootstyle="danger"`, `bootstyle="primary"`)

### 功能改动 (control.py)
- NIC 优先级排序: USB 网卡 > 169.254 网段 > 内置网卡
- 单磁盘自动选择: 只有一个磁盘时自动选中
- DHCP 按钮: 重命名为"寻找旧电脑"
- 手动 IP: 改用单输入框 (tk_entry_const), 作为高级选项可折叠
- 验证码: 自动大写转换 + 限制 4 位
- 角色选择回调: `_on_role_selected` 替代旧的 combobox 方式
- 步骤导航: 上一步/下一步按钮逻辑
- 设备发现下拉框: 显示计算机名和 IP
- 进度条标签: "总进度" 和 "当前分区进度"
- 开始按钮: 自动跳转到传输进度页面 (步骤 4)

### Bug 修复
- `control.py:166` — `_current_mode` 未定义导致 `AttributeError`, 改为 `_populate_disks()` 无参调用
- `ui.py:210` — ttk.Frame 不支持 `-bd` 选项, 改用 `_tk.Frame(bd=2, relief=SOLID, ...)` 
- `build.ps1` — 添加 ttkbootstrap/PIL hidden-import 和 collect-data

### 构建结果 (最终)
- 编译成功: 磁盘拷贝工具.exe (22.2 MB, 20.9 秒)
- 测试: EXE 正常启动, main.py 无报错

### UI 风格恢复 (参照 页面展示.pptx)
- 从 PPTX 提取了 3 张 UI 截图 + theme1.xml 配色方案, 全面恢复了设计风格:
- **顶部标题栏**: 左 "磁盘拷贝工具" (16pt bold) + 右 "当前角色：发送方/接收方" (蓝色, 10pt)
- **右侧步骤指引**: 灰色背景侧边栏, 3 步显示 (选择设备类型/选择磁盘与分区/开始传输)
  - 完成: 绿色圆点 + 绿色文字 + 绿色描述 (如 "已选择：发送方")
  - 当前: 蓝色圆点 + 蓝色文字 + 详细描述
  - 待处理: 灰色圆点 + 灰色文字 + 灰色提示
- **角色卡片**: "发送方" (源设备-旧设备) / "接收方" (目标设备-新设备) 
  - 白色卡片 + 灰色边框, 鼠标悬停高亮 #f5f5f5
  - 按钮改用 dark primary 样式
- **磁盘页**: 分区盘符映射 (D/E/F > 下拉框), 运行环境 (WinPE/正常系统) radio buttons
- **连接页**: 深色控制台 (#2B2B2B), 验证码红色大字 (28pt Consolas, 红色 #D32F2F 白底), 
  - 发送方 "准备就绪" 居中 + 验证码框, 接收方 "寻找旧电脑" + 高级手动 IP + CSV 选择
- **传输页**: 总进度(绿色) + 分区进度(蓝色) + 深色日志控制台, 滚动条 dark-round
- **底部状态栏**: tk_label_status (灰色文字, 8pt, 侧边栏同色背景)
- **导航按钮**: "< 上一步" secondary-outline, "下一步 >" primary, "开始传输" success
- 补充: hide_auth_input/show_auth_input (接收方/发送方切换)
- 构建: 22.2 MB, 14.2 秒, EXE 测试正常

### 核心功能验证 (2026-07-30)
- **验证码生成** (tls_utils.generate_auth_code): 4 位字母数字, 20 次生成 20 个不同结果 → PASS
- **SSL/TLS 证书** (get_or_create_fixed_cert): SAN 含 169.254.100.1 + 127.0.0.1 + 主机名, 客户端 CERT_NONE → PASS
- **HTTPS 文件传输** (端到端测试): /ping /list /get /batch_get 全部通过, 错误验证码 403 拒绝 → PASS
  - 服务器: FileServer 基于 http.server + SSLContext
  - 客户端: urllib.request + ssl.create_default_context(CERT_NONE), 所有请求带 ?pwd=<验证码>
  - _send_json: Content-Length 已正确设置
  - _download_batch: 5 元组正确解包
- **磁盘扫描** (disk_scanner): get_disk_list() 正常返回, get_partition_details() 返回分区盘符列表 → PASS
- **网卡扫描** (nic_scanner): USB/169.254 优先级排序正常 → PASS
- **verifier.py**: 已正确使用 auth_code + HTTPS + CERT_NONE → PASS

### UI 适配 1280x700 低分辨率低色域
- 窗口尺寸: 1280x700 (从 980x640)
- 侧边栏宽度: 200px (从 240px)
- 字体大小: 标题 14pt (从 16pt), 正文 9pt, 辅助 8pt
- 间距/内边距缩减: padx 40→14, pady 28→20 等
- 低色域优化: 所有灰色加深 (C_GRAY_BG #e9ecef→#ddd, C_TEXT_SEC #666→#555, C_SEP #e0e0e0→#ccc)
- 验证码字体: 28pt→24pt Consolas
- 构建: 22.2 MB, 17.0 秒, EXE 正常启动
