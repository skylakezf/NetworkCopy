# 2026-07-28

## 全项目 Bug 审查 + 改进调研（未改代码，仅报告）

### 致命 Bug（当前主流程无法工作）
1. control.py 与 file_transfer.py 接口脱节：
   - `FileServer(...)` 未传 auth_code / cert_paths → ssl_ctx=None → `get_request` 抛 RuntimeError，服务器拒绝所有连接；auth_code="" → 所有请求 403。
   - `download_files` / `scan_source_device` 未传 auth_code → ping 403 → "无法连接源设备"。
   - control.py 完全没有生成/显示/输入验证码的逻辑（tls_utils 未被 control 引用）。
2. `_send_json` (HTTP/1.1 keep-alive) 未发送 Content-Length → 客户端 /ping /list read() 挂起直到超时 → JSON 端点全部失败。
3. `_download_batch` 4 处 except 分支 `for rel_path, fsize, target_path in remaining_tasks` 解包 3 值，但元素是 4 元组 (含 mtime) → ValueError。
4. verifier.py `_retry_missing_files` / `_download_one_file` 用 http:// 明文 + 无 pwd → 服务器 TLS+403 必然失败。
5. control.py `run_verification` 未传 server_ip / gtmc_new_name → 重试下载永不触发；且源端已把 GTMC_User_Profiles 重命名为带日期后缀，CSV 未 patch → 该目录全部误报 N。

### 其它 Bug
- `_resolve_path` 路径穿越：normpath 后 "..\\.." 前缀未拦截。
- nic_scanner `_format_speed` Gbps 分支输出 "1.0GbpsGbps"（rstrip 后又拼 "Gbps"）。
- netsh 回退：列表参数里又加了引号 `f'"{desc}"'`，且传的是 Description 而非连接名 → netsh 必失败。
- UI 设备下拉框选择的 IP 未被使用，`_connect_and_download` 硬编码 DHCP_ASSIGNED_IP=169.254.100.2；DHCP 首租约被他机抢走时连错设备。
- `_pause_event.clear(); wait()` 无人 set → "pause" 会死锁（当前死代码）。
- progress_reporter 线程在有 errors 时永不退出（completed<total）。
- dhcp_server `wait_for_client` docstring 说返回 (success, ip) 实际返回 bool。
- JSON 用 utf-8-sig 编码（带 BOM，违反 RFC 8259）。
- ip_config SOURCE_IP/TARGET_IP 语义与 control.py 实际角色相反（易误用）。

### 改进方案（调研结论）
- JSON 响应补 Content-Length（或 Connection: close）。
- 大量小文件：改用 tarfile 流式打包 over HTTPS（server 边 walk 边写 wfile，client 边收边解包），替代自制 batch 二进制协议，减少往返。
- 大文件断点续传：HTTP Range。
- 写文件用临时文件 + os.replace 原子替换。
- 校验增加流式 zlib.crc32（比仅 size 可靠，比 MD5 快）。
- MEMORY.md 已更新：端口实为 9999（非 443/80）。

## P0 修复（已完成，py_compile 通过）
1. file_transfer.py：`_send_json` 补 Content-Length（改用 utf-8 无 BOM）；`_download_batch` 4 处 except 回退分支改为 4 值解包 `(rel_path, fsize, target_path, _mt)`。
2. verifier.py：新增模块级 `_SSL_CTX`（CERT_NONE）；`_retry_missing_files`/`_download_one_file` 改 https + `?pwd=` + context，均新增 `auth_code` 参数；`run_verification` 新增 `auth_code` 参数并透传。
3. control.py：
   - import tls_utils / simpledialog / messagebox；__init__ 预置 `_use_dhcp/_source_ip/_dhcp_server/_auth_code/_last_source_ip`。
   - 源端 `_start_source_server`：`tls_utils.generate_auth_code()` + `get_or_create_cert(get_local_ip() or DHCP_ASSIGNED_IP)` 传入 FileServer；验证码经日志/状态栏/messagebox 显示；证书失败则中止。
   - 目标端 `_start_target_download`：simpledialog 输入验证码（strip+upper）；scan_source_device/download_files 均传 auth_code；连接成功后记录 `_last_source_ip`。
   - 校验 `_start_verification`：传 server_ip/gtmc_new_name/auth_code；新增 `_detect_gtmc_new_name()` 在目标 D 盘检测被源端重命名的 GTMC_User_ProfilesYYMMDD 目录（取 mtime 最新）。
- lint 噪音：control.py 原有 basedpyright 严格模式报错（ui: object 无注解），非本次引入。
- 未修（P1/P2）：路径穿越、_format_speed 双 Gbps、netsh 引号、UI 选中 IP 未用、pause 死锁、进度线程不退出、原子写入、CRC 校验、tar 流式传输。

## 分区检测 + 按钮分离修复（已完成，py_compile + 实测通过）

### 1. 分区检测/自动映射失效（"检测到0个可用分区"）
- 根因：disk_scanner.py `get_partition_drive_mapping` 解析 `VOLUME_DISK_EXTENTS` 时结构偏移错误。
  - `VOLUME_DISK_EXTENTS` 在 `NumberOfDiskExtents`(4字节) 之后有 4 字节对齐填充，首盘区从 offset **8** 开始（非 4）。
  - 每个 `DISK_EXTENT`：`DiskNumber` 在相对 offset 0，`StartingOffset`(LARGE_INTEGER) 在相对 offset **8**（前面 4 字节 DiskNumber + 4 字节对齐填充），非 4。
- 原代码 `ext_offset = 4 + ei*24` 且 `StartingOffset` 读 `ext_offset+4` → 每个卷的 ext_start 恒为 0 → 与分区偏移匹配失败 → 映射返回 {} → 自动填充 0 个、按钮不自动点亮。
- 修正：`ext_offset = 8 + ei*24`，`DiskNumber` 读 `ext_offset+0`，`StartingOffset` 读 `ext_offset+8`。实测 PhysicalDrive0 现在返回 `{3:'C',4:'D',5:'E',6:'F'}`，count=4。
- 附带改进 control.py `_auto_fill_drive_mapping`：优先同名盘符匹配（源 D→目标 D），否则按序跳过首个系统分区补齐；调用处改为传完整 part_map。

### 2. DHCP 与开始传输按钮分离（目标设备流程：开启DHCP → 源设备分配到IP → 开始接收）
- ui.py：新增 `tk_button_dhcp`（"开启DHCP"，x=372,y=165,w=108）；发现设备下拉框宽度 380→265 让出空间；新增 `show_dhcp`/`hide_dhcp`。
- control.py：
  - `_setup_events` 绑定 `tk_button_dhcp` → `_on_dhcp_button`。
  - `_setup_ui_defaults` 初始 `hide_dhcp()`。
  - `_on_device_type_selected`：目标设备 `show_dhcp()`，源设备 `hide_dhcp()`。
  - `_configure_ip`：目标设备**不再自动启动** DHCP（改为提示用户点击按钮）。
  - 新增 `_on_dhcp_button`：校验网卡已选 → 禁用按钮+"DHCP启动中..." → 线程调 `_setup_target_dhcp`。
  - `_setup_target_dhcp`：`try/except` 包裹服务器启动；成功按钮转 "DHCP运行中"，失败/异常复位为 "开启DHCP"。
  - `_start_target_download` 开头新增守卫：未开启 DHCP（或服务器未运行）则拒绝并提示。
  - `_on_transfer_failed`：恢复 `tk_button_dhcp` 为 "开启DHCP"。
- 注：lint 0 错误；py_compile 通过。

## 自动映射改为按物理分区顺序（已完成，py_compile + 实测通过）
- 用户规则：盘符可能乱序（4 分区物理序 CDEF 可能挂载为 CEFG；3 分区机械盘可能挂载为 EDF），映射必须按分区在磁盘上的物理顺序，兼容 GPT/MBR。
- disk_scanner.py：新增 `get_partition_details(disk_number) -> [(starting_offset, part_number, letter), ...]`，按 StartingOffset 排序（MBR 逻辑分区号不连续，故不用分区号排序）；`get_partition_drive_mapping` 改为其 wrapper；函数内早退分支 `return {}` 全部改 `return []`。
- control.py `_auto_fill_drive_mapping(ordered_letters, ntfs_count)` 重写：
  1. 输入为物理顺序盘符列表；2. 去掉盘符 C（系统分区）；3. 剩余 >3 个则从头部跳过（视为系统/保留分区）；4. 按物理顺序依次填充 D→E→F。
  - 例：C,E,F,G → D=E, E=F, F=G；三分区 E,D,F（物理序）→ D=E, E=D, F=F。
- `_on_disk_selected` 调用处改为 `get_partition_details` 并传物理顺序盘符列表。
- 实测 PhysicalDrive0：details=[(offset,3,'C'),(...,4,'D'),(...,5,'E'),(...,6,'F')]，wrapper map 输出不变。

## 验证码 UI 醒目显示（已完成，py_compile 通过）
- 需求：连接验证码是重要信息，不能只在 log 中出现。
- ui.py：新增 `tk_label_auth_title`（"连接验证码"标签，x=340,y=195）+ `tk_label_auth_code`（原生 tk.Label，Consolas 16 bold，红字 #D32F2F / 橙底 #FFF3E0，groove 边框，x=428,y=190,w=187,h=36，位于手动 IP 行右侧空白区）；Win 类新增 `show_auth_code(code)` / `hide_auth_code()`。
- control.py：`_setup_ui_defaults` 初始隐藏；`_on_device_type_selected` 切换设备类型时清空 `_auth_code` 并隐藏；源设备 `_start_source_server` 生成验证码后 `show_auth_code` 常驻显示（弹窗保留并注明主界面也有）；目标设备 `_start_target_download` 输入验证码后也 `show_auth_code` 便于核对。
- 注：basedpyright 报的 attribute-unknown 均为既有告警（ui 参数无类型注解），非本次引入。

## 手动 IP 文本框限制 0~255 + 开始按钮常驻禁用修复（已完成，py_compile 通过）
- 需求：手动输入 IP 的 4 个文本框每个只能输入 0~255；开始传输按钮一直不可用需修复。
- ui.py：
  - 新增 `_octet_vcmd = (self.register(self._octet_validate), "%P")`（在 `__win()` 后注册）。
  - `__tk_ip_octet` 增加 `validate="key", validatecommand=self._octet_vcmd`。
  - 新增 `_octet_validate(new_value)`：允许空串；拒绝非数字、>3 位、数值 >255。输入时即拦截（如 "259" 被拒）。
- control.py：
  - 根因：`_check_button_state()` 仅在分区检测成功路径与分区 combobox 变更时调用；网卡/设备类型选择后从不重算，且分区检测若在 PE 抛异常会在 reach 启用检查前 return → 按钮永久禁用。
  - 修复：`_on_nic_selected` 末尾、`_on_device_type_selected` 末尾各加 `self._check_button_state()`；`_detect_partitions` 用 try/finally 包裹，finally 中 `self.ui.after(0, self._check_button_state)` 确保无论成功失败都重算。
  - `_check_button_state` 放宽分区数要求：_ntfs_partition_count==2 需 2 个映射，==3/4 需 3 个，==0（未知/检测失败）放宽到至少 1 个映射，避免永久禁用（真正完整性校验仍在 `_on_start_button` 中再次进行）。
- 注：手动 IP 文本框当时仅做输入校验，尚未接入连接逻辑（源/目标走 DHCP/169.254.x.x）。

## 手动 IP 接入传输 + 按钮彻底解禁（已完成，py_compile 通过）
- 需求：手动输入 IP 也要能传输；开始传输按钮仍持续禁用；源设备按钮只搭 HTTP 服务器，不必太多限制。
- ui.py：标签 "手动输入IP:" → "源设备IP(手动):"（该 IP 即源设备 IP，两端填同一值）。
- control.py：
  - `_check_button_state` 彻底改为：只要「网卡已选 + 设备类型已选(源/目标)」即启用按钮；真正的完整性校验（分区映射/DHCP/手动IP）放到 `_on_start_button` 点击时。彻底消除「永久禁用」。
  - 新增 `_get_manual_ip()`：4 段均数字且 0~255 返回 "x.x.x.x" 否则 None。
  - 新增 `_apply_manual_ip(adapter_desc, ip)`（源设备：把本机网卡设为该 IP）、`_apply_target_manual_ip(adapter_desc, source_ip)`（目标：把本机设为同网段 源末位+1，便于直连）。均优先 `set_ip_via_api`，失败 fallback `netsh`。
  - `_on_start_button` 重写：先算 manual_ip；源设备不卡分区映射（仅提示），调 `_start_source_server(manual_ip=)`；目标设备仍需分区映射完整，调 `_start_target_download(manual_ip=)`。
  - `_start_source_server(self, manual_ip=None)`：manual_ip 非空时先设本机 IP 再起服务器（服务器绑 0.0.0.0，故直连可达）。
  - `_start_target_download(self, manual_ip=None)`：重写；manual_ip 非空则跳过 DHCP 守卫、直接 `_apply_target_manual_ip` 并用该 IP 直连；否则仍走 DHCP（169.254.100.2）/APIPA 扫描逻辑。
- 注：basedpyright 既有告警（ui:object 导致 tk_* 全报 unknown）依旧，非本次引入；`Literal` import 现已未使用（warning，无害）。

## 验证码/手动 IP UI 调整（已完成，py_compile 通过）
- 用户疑问：①源设备"手动输入IP"留着干什么（源设备不涉及IP获取）；②源/目标设备验证码的展示与输入框在哪。
- 结论与改动：
  - 手动 IP（"源设备IP(手动)"4 段输入框）默认隐藏（`hide_manual_ip()`），且源设备切换时显式 `hide_manual_ip()`。源设备由目标 DHCP 分配 IP（169.254.100.2），无需手动设 IP；该框当前为隐藏状态，仅保留 `show_manual_ip()` 备用。
  - 验证码：源设备=**展示**（主界面右上红色"连接验证码"框 + messagebox 弹窗）；目标设备=**输入**（新增主界面左侧 `tk_entry_auth_input` 输入框，"连接验证码:" 标签，x=118）。
  - ui.py：新增 `tk_entry_auth_input` + `tk_label_auth_input_title`；新增 `_auth_vcmd`(4位英文数字校验)、`_auth_validate`、`show_auth_input/hide_auth_input`、`get_auth_input/set_auth_input`、`show_manual_ip/hide_manual_ip`。
  - control.py：`_setup_ui_defaults` 初始 `hide_manual_ip()+hide_auth_input()`；`_on_device_type_selected` 源→`hide_manual_ip()+hide_auth_input()`，目标→`show_auth_input()`；`_start_target_download` 优先读主界面 `get_auth_input()`，为空才弹出 simpledialog 兜底；修复 `_get_manual_ip` 引用不存在的 `tk_input_ip_part1..4`（应为 `ip_octet1_var..4`），否则运行即 AttributeError。
- 注：手动 IP 直连功能现仅代码层面保留（UI 隐藏），主流程走 DHCP。如以后需要目标直连已知源 IP，可重新 `show_manual_ip()`。

## WinPE 运行环境单选 + 重命名保护（已完成，py_compile 通过）
- 用户要求：①`_rename_gtmc_user_profiles()` 仅当运行在 WinPE 下才重命名 GTMC_User_Profiles，正常系统不改动用户文件夹；②预留一个单选框供用户选择运行环境是否 WinPE。
- 实现：
  - ui.py：新增 `winpe_var = StringVar()` + 两个 Radiobutton（`tk_radio_winpe` value="winpe" / `tk_radio_normal` value="normal"），标签"运行环境"，置于右上角空白区 (x=505, y=70/92/112)。
  - control.py：新增模块级 `is_running_in_winpe()`（依据注册表 `HKLM\SYSTEM\CurrentControlSet\Control\MiniNT` 或 `X:\Windows\System32` 判定）；`_setup_ui_defaults` 里按检测结果 `winpe_var.set("winpe"/"normal")` 预选，用户可手动改。
  - `_rename_gtmc_user_profiles()` 开头加守卫：`if self.ui.winpe_var.get() != "winpe": 跳过并记录日志`。
- 注意：单选框为全局设置（源/目标设备均可见），仅影响源设备的重命名行为。

## 校验逻辑同步 WinPE 条件（已完成，py_compile 通过）
- 用户要求：以前软件纯 WinPE 运行→源端必重命名 GTMC_User_Profiles，校验也随之改 CSV；现在重命名已改为仅 WinPE 才做，校验逻辑须一并条件化。
- verifier.py 改动：
  - 新增本地 `is_running_in_winpe()`（与 control 同逻辑，本地副本避免循环导入）。
  - `run_verification` 新增参数 `winpe: bool|None=None`；为 None 时自动检测。仅当 `winpe and gtmc_new_name` 才执行 `_patch_csv_gtmc_paths` 把 CSV 中 `GTMC_User_Profiles`→新名；`not winpe` 时明确跳过并记日志。
  - `_patch_csv_gtmc_paths` 增加幂等保护（`new_name in content` 也直接 return），防止重跑时后缀被重复拼接。
- control.py：调用 `run_verification` 时传入 `winpe=(self.ui.winpe_var.get()=="winpe")`，使校验与源端重命名采用同一环境判断。
- 说明：`_detect_gtmc_new_name` 仍按目标 D 盘实际目录名探测 gtmc_new_name；winpe 门控使其与重命名决策一致。

## Appl 文件夹新命名 + 接收端 CSV 手动选择（已完成，py_compile + 解析测试通过）
- 背景：配置文件修改后，Appl 下文件目录名由纯日期（YYYY/MM/DD）改为前缀+日期，例如：
  - `QDNBS537_2026-07-28`、`DESKTOP-K5JBCND_2026-07-07`（格式：<主机名>_YYYY-MM-DD）。
- 需求：①自动识别这些目录并选取最新的 FullFilelist_DEF.csv；②接收端增加一个文件选择框，可手动指定 FullFilelist_DEF.csv。
- verifier.py 改动：
  - `_parse_date_from_folder` 重写：先试整体纯日期；再取最后一个 `_` 后的子串试日期（兼容 `HOSTNAME_YYYY-MM-DD`）；最后兜底正则搜索任意位置 YYYY-MM-DD/YYYYMMDD。已用临时脚本验证 6 种样例解析正确。
  - `find_latest_appl_folder` 仍按 max(date) 选最新 → 新命名下自动选到日期最大者（如两个样例选 QDNBS537_2026-07-28）。
  - `run_verification` 新增参数 `csv_path: str=""`：非空且文件存在则直接采用该 CSV、跳过 Appl 最新目录自动识别；为空则原自动识别流程。
- ui.py 改动：
  - 新增接收端 CSV 选择框：`csv_path_var=StringVar()` + `tk_label_csv`("FullFilelist_DEF.csv:"，x=20,y=233) + `tk_entry_csv`(readonly 显示路径，x=165,w=355) + `tk_button_browse_csv`("浏览..."，filedialog.askopenfilename 限定 *.csv，x=525,w=85)。三者 y≈231，h≈26。
  - 为给该行腾位置，窗口高度 640→680，并将 status/progress×2/log标签/文本/滚动条 整体下移 30px（status 235→265，progress 260→290，file_progress 288→318，log 318→348，text 346→376，滚动条 346→376）。
  - Win 类新增 `show_csv_selector()/hide_csv_selector()/get_csv_path()/set_csv_path()`。
- control.py 改动：
  - `_setup_ui_defaults` 初始 `hide_csv_selector()`。
  - `_on_device_type_selected`：目标设备 `show_csv_selector()`，源设备 `hide_csv_selector()`。
  - `_start_verification`：读 `csv_path = self.ui.get_csv_path()`，记录日志，并透传给 `run_verification(csv_path=csv_path)`（空则自动识别）。
- 注：basedpyright 既有 attribute-unknown 告警（ui:object 无注解）依旧，非本次引入；3 文件 py_compile 通过。

## 接收端重新启用「手动输入源设备IP」直连（已完成，py_compile 通过）
- 背景：上一轮把手动 IP 在源/目标两端都隐藏了；用户指出接收端必须保留手动 IP 直连——当对方（源设备）网卡已有 IP、不经 DHCP 时，没有此功能就无法传输。
- 实际逻辑早在前序已实现并接线（`_get_manual_ip`、`_apply_target_manual_ip`、DHCP 守卫在 manual_ip 非空时跳过），只是 UI 被隐藏。本次仅重新开放 UI。
- control.py `_on_device_type_selected`：目标设备由 `hide_manual_ip()` 改为 `show_manual_ip()`（源设备仍 `hide_manual_ip()`）。
- ui.py 布局调整（避免手动 IP 行与验证码输入行重叠）：
  - 手动 IP 行整体下移：`show_manual_ip` 坐标 label/dot y=195→228，octet y=193→226（x 不变）。
  - CSV 选择行随之下移：label y=233→264，entry/button y=231→262。
  - 窗口高度 680→710；底部 status/progress×2/log标签/文本/滚动条 再整体下移 31px（status 265→296，progress 290→321，file_progress 318→349，log 348→379，text 376→407 h=283，滚动条 376→407 h=283）。
- 行为：目标端选「目标设备」后显示 验证码输入框 + 源设备IP(手动)4 段框 + FullFilelist_DEF.csv 选择框。手动 IP 留空→走 DHCP；填入→直连该源 IP（自动设自身同网段末位+1），无需开启 DHCP。
- 注：lints 仍为项目既有 basedpyright（ui:object）噪音，非本次引入；py_compile 通过。

## 排查「服务能打开但文件无法传输」并修复（已完成，py_compile + 独立复现测试通过）
- 用户现象：接收端经 DHCP 给发送端分配了正确 IP，浏览器能打开发送端 HTTP 服务，但文件无法传输。
- 排查方法：用嵌入式 Python 写独立复现脚本（file_transfer.py 独立起 FileServer + download_files），证明**传输核心逻辑正确**：
  - 客户端(HTTPS)能连通、能列目录、能完整拷贝含中文/子目录/大文件（3/3 成功）。
  - 服务器确为纯 TLS：TLS 客户端 200 成功；明文 HTTP 客户端被拒（ConnectionReset）——排除"服务器其实是明文"假设。
  - DHCP 拓扑一致：MiniDHCPServer 从 169.254.100.2 起分配，control.DHCP_ASSIGNED_IP="169.254.100.2"，目标自身设 169.254.100.1 后连 169.254.100.2。
- 找到并修复 2 个真实缺陷：
  1. **TLS 握手头读取竞态** (file_transfer.py ThreadingFileServer.get_request)：原 `conn.recv(2, MSG_PEEK)` 在跨机网络下可能只先到 1 字节，误判为明文 HTTP 直接 `conn.close()`+抛错，导致 Python 客户端单次连接失败（浏览器会重试故"能打开"）。改为循环补齐到 2 字节 (0x16 0x03) 再判定。已在独立测试中复跑仍 3/3 通过。
  2. **源端静默 0 文件** (control.py `_on_start_button` 源分支)：原仅打"提示"日志仍启动服务器 → 若源端未选盘符则对外提供 0 文件，表现正是"服务开了却传不了"。改为**强制校验**：源端 `_partition_map` 为空则报错返回，要求先选 D/E/F。并在 `_start_source_server` 启动后新增日志 `将对外提供以下盘符数据: {...}` 便于排查。
- 其余核对无误：partition_map 两端 key 均为 D/E/F（源端=源自身盘，目标端=目标盘），auth_code 经 compare_digest 类型安全，运行时 source_ip 走 DHCP 常量，scan_source_device 仅 APIPA 模式使用。
- 结论：非我前序 UI 改动导致（UI 不影响传输）。修复后重编译通过；未运行真实双机场景，但核心路径已有独立测试背书。
- 注：lints 仍为项目既有 basedpyright（ui:object）噪音，非本次引入。

## 重大 Bug：FileServer 遇非法连接即整体崩溃（已修复，端到端测试 PASS）
- 用户现象：源端浏览器访问 `127.0.0.1:9999` 与 `https://127.0.0.1:9999` 均无法加载；实际是服务器启动后一被连接就死，等价于"服务器没搭起来"。
- 排查方法：新增独立端到端测试 `test_transfer_e2e.py`（保留在仓库根，可直接用嵌入式 python 运行）。测试先起真实 `FileServer`(partition_map D→临时源目录)，探测端口监听，再发明文请求试探、最后用真实 `download_files` 拉取并逐字节比对。
- 关键复现结果：服务器**确实能启动并监听 9999**（TCP 建立成功）；但测试脚本先发的"明文请求"被 `get_request` 以 `raise RuntimeError` 拒绝，该 `RuntimeError` 未被 `BaseServer.handle_request` 捕获（基类只捕获 `except OSError`），异常逃逸出 `serve_forever`，**整个服务器线程崩溃退出** → 后续 HTTPS 客户端再连即握手超时。
- 根因：浏览器访问时往往先探测 `http://`（明文），或任意非干净 TLS 握手，都会触发 `get_request` 抛 `RuntimeError` 把服务器打死；一旦死，连 `https://` 也连不上——完美复现用户现象。
- 修复（file_transfer.py `ThreadingFileServer.get_request`，约 314~361 行）：把所有拒绝/失败路径的异常类型由 `RuntimeError` 改为 `OSError` 子类 `ConnectionAbortedError`（含：无证书、peek 循环异常、非 TLS(明文)、TLS 握手失败）。这样非法连接只会被丢弃，服务器继续存活。
- 复测结果：明文试探被拒且**服务器不崩溃**，HTTPS 客户端成功下载 3/3 文件(2.0MB)，逐字节比对 PASS。`py_compile` 通过；lints 仍为项目既有 basedpyright 噪音。
- 重要澄清给用户：本服务器是**纯 TLS(HTTPS)**，默认 `http://127.0.0.1:9999`（明文）按安全策略(指令 C)被拒是**设计如此**，浏览器须用 `https://127.0.0.1:9999` 并手动接受自签名证书告警（无 pwd 时返回 JSON/404，能"加载"）。真实双机传输走工具自带 HTTPS 客户端，不依赖浏览器。
- 注：测试脚本 `test_transfer_e2e.py` 故意先发明文再发 HTTPS，目的是验证"被明文击中后服务器仍存活"；该脚本可作为回归测试保留。

## 改为"固定证书 + 大量诊断日志"（用户明确要求）
- 用户反馈：本地 `https://127.0.0.1:9999` 浏览器无法加载、API 无法 GET。根因：旧证书按 `get_local_ip()` 的局域网 IP 生成，SAN 不含 `127.0.0.1`，浏览器连本地时主机名不匹配。
- 改动1（tls_utils.py）：新增 `get_or_create_fixed_cert()` 生成 **一份固定证书** `certs/server.pem`/`server.key`，SAN=`[127.0.0.1, localhost, 主机名]`，与 IP 无关，服务器一律使用它；`generate_self_signed_cert` 增加 `san_list` 参数。`build.bat` 增加 `--add-data "certs;certs"` 把证书随 exe 打包。
- 改动2（control.py `_start_source_server`）：不再用 `get_or_create_cert(cert_ip)`，改为 `get_or_create_fixed_cert()`；启动日志新增证书路径、SAN、本地测试地址提示（`https://127.0.0.1:9999`，浏览器点"高级→继续访问"，API 需带 `?pwd=<验证码>`）。
- 改动3（file_transfer.py）：新增大量 `[诊断]` 日志 —— `FileServer.start` 打印证书文件/SAN/验证码是否设置/映射盘符；`get_request` 打印每个连接来源、明文拒绝、TLS 握手成功/失败；`do_GET`/`do_POST` 打印未授权请求（缺/错 pwd）。
- 验证：`test_fixed_cert.py` 证明"信任该证书+校验主机名"的浏览器等价客户端连 `https://127.0.0.1` 与 `https://localhost` 均 200；接收端 `CERT_NONE` 传输正常；`test_transfer_e2e.py` 端到端 3/3 文件 PASS；`py_compile` OK。
- 重要：自签名证书浏览器仍会报"不受信任"警告，需手动点"继续访问"——这是自签名固有限制（安全性由随机 pwd 保证，非证书）。固定证书方案已消除"主机名不匹配"这一额外拦截。
- 注：`certs/` 下旧的 `127.0.0.1.*`/`192.168.56.1.*` 已不再被使用（兼容函数 `get_or_create_cert` 保留），如需整洁可删除。

## build.bat 编码修正（exe 名乱码 纾佺洏鎷疯礉宸ュ叿.exe）
- 现象：构建产物名变成 `纾佺洏鎷疯礉宸ュ叿.exe`（UTF-8 字节按 GBK 解读的典型乱码）；后又出现 `\ufffd` 坏名 exe。
- 根因：原 `build.bat` 被写成 **UTF-8**，而中文 Windows 的 `cmd.exe` 按 **GBK(代码页936)** 解读批处理内容，于是 `--name "磁盘拷贝工具"` 的 UTF-8 字节被当成 GBK → 乱码。
- 用户结论（正确）：`.bat` 必须保存为 **GBK** 编码。已用嵌入式 python 将 `build.bat` 转 GBK，并在顶部加 `@chcp 936 >nul` 强制用 GBK 读批处理（增强健壮性，防个别机器开"UTF-8 全球支持"Beta 导致代码页变 65001）。校验：`磁盘拷贝工具` 在 GBK 读取下正确、`纾佺` 不存在。
- ⚠️ 重要坑（供以后参考）：本会话的**执行沙箱会强制注入 `WSL_UTF8=1`**，使嵌入式 Python 进入 UTF-8 模式。在该沙箱里无论 `.bat` 何种编码，构建产物名都会变成 `\ufffd` 坏名（cmd 以 GBK 传参、Python 以 UTF-8 解码 → 替换符）。这**不代表用户真实环境**——用户的 `纾佺...` 乱码恰恰证明他那边是标准 GBK 模式 Python（无 UTF-8 强制）。在沙箱模拟需 `cmd /c "set WSL_UTF8= & ..."` 关掉该变量回到 GBK 模式（已验证 `utf8_mode=0`），但即使如此 PyInstaller 构建仍可能因沙箱变量残留而产出 `\ufffd`，故**沙箱内无法干净复现用户的成功构建**，应以"GBK .bat + 用户标准中文 cmd"为正确结论。
- 已清理 dist/ 中沙箱产生的两个坏名 exe（`纾佺...exe` 与 `\ufffd` 坏名 exe），保留 `磁盘拷贝工具.exe` 与 `磁盘拷贝工具_back.exe`。
- 注意：任何用 write_to_file 重写 `build.bat` 都会变回 UTF-8，需重新用 python 转 GBK（命令见上）。

## 接收端"连不上源服务器"诊断（用户反馈：浏览器能访问源端，接收端点接受后没访问到）
- 用户疑问："是接收端没有自动忽略证书错误吗？" → **不是**。证书忽略已实现：`file_transfer.py` 的 `CLIENT_SSL_CTX = tls_utils.make_client_ssl_context()`（CERT_NONE）+ `verifier.py` 的 `_SSL_CTX`（CERT_NONE）都已关闭证书/主机名校验。浏览器能访问也印证了源服务器本身正常。
- 真正根因（推测）：网络层不通或连接目标 IP 不对。接收端 `download_files` 旧逻辑 ping 失败只打 `无法连接到源设备: {e}`，把底层原因（超时/拒绝/主机不可达/证书错）吞掉了；且 `control.py._connect_and_download` 缺少"本机IP/目标IP/TCP端口可达性"自检，无法判断是连错IP还是网不通。
- 本次新增诊断代码：
  1. `file_transfer.py._urlopen_https`：包裹 urllib，连接失败时按 errno 分类打印 `[超时/连接被拒绝/主机不可达/TLS错误]`，并带 `log_callback` 打出 `[诊断]`。
  2. `file_transfer.py.download_files` ping 循环：每次失败打印 `第N/5次连接失败: 类型: 错误`；连通但非ok也打印；最终失败时给"排查建议"（源端是否启动/网线/同网段/防火墙9999/验证码）。启动处额外打印 base_url、验证码是否携带、客户端 TLS 上下文 check_hostname/verify_mode（验证确为 CERT_NONE）。
  3. `control.py._connect_and_download`：连接前做网络自检——打印拟连接源IP、本机所有网卡IP、对 `source_ip:9999` 做 TCP 端口可达性探测（create_connection），不可达时提示检查源端/网线/同网段/防火墙。
- 验证：`py_compile` 三个文件通过。lint 报错均为 basedpyright 类型推断噪音（Unknown/动态UI属性），非运行时问题、非本次引入。
- 待用户复现：让用户重新"接受"并贴出含 `[诊断]` 的日志，即可定位是"连错IP（本机IP与目标不在同网段）"还是"端口不可达（源端服务器没起/防火墙）"。

## 根因锁定 + 修复：掩码非对称导致接收端连不上源端（用户三个问题同源）
- 用户三个问题（源/接收掩码不一致 / 点接受没选IP且重复打印分区映射 / 超时）**同源**：原方案用 `169.254.x.x` 但把静态IP与DHCP都设成 `/24`(255.255.255.0)；而 169.254.x.x 是 APIPA 地址段，**天生 /16(255.255.0.0)**。当接收端用 `AddIPAddress` 设静态IP失败（日志 `API 参数无效(网卡可能未启用)`，ret=87）时，网卡回退 APIPA 得到 `169.254.116.119/16`。于是源端 /24、接收端 /16 **非对称**：接收端能发往源端，但源端回包发现 169.254.116.119 不在自己 /24 网段、又无网关 → 丢弃 → 连接超时。证书忽略(CERT_NONE)确认与问题无关。
- 修复（统一 /16，与 APIPA 一致）：
  1. `ip_config.py`：`SUBNET_MASK = "255.255.0.0"`（原 255.255.255.0），注释说明 /16 原因。
  2. `dhcp_server.py`：`DHCP_MASK = "255.255.0.0"`，DHCP 下发给源的掩码随之变 /16。
  3. `control.py`：`_apply_target_manual_ip`/`_apply_manual_ip`/`_setup_target_dhcp` 改用 `set_ip_via_netsh`（内部 GBK 解码、默认 /16）作回退，且**诚实上报成功/失败**（不再失败时谎报"目标手动 IP: X"）；删除源/接收端各一处冗余的 `分区映射` 打印（原 `_on_start_button` 已打印过，导致每次点击重复两行）。
- 修复后效果：即便接收端静态IP设置失败停在 APIPA(/16)，因源端也是 /16，两端同处 169.254.0.0/16 可直接通信，超时问题解决。

## 新问题（7/29）：ping 通但 TCP 9999 超时 → 源端防火墙拦截
- 用户复现: 接收端 `API 参数无效(网卡可能未启用)`(AddIPAddress ret=87, 持久)、`netsh 失败`、本机IP `169.254.116.119`/`192.168.152.139`；`[诊断] TCP 端口 169.254.91.39:9999 不可达: TimeoutError`；但**ping 能通**、**源端本机浏览器能访问**。
- 根因: 源端 Windows 防火墙默认拦截入站 TCP 9999。ICMP(ping)被放行→能通；TCP SYN 被丢弃→超时(非拒绝)；浏览器在源端用 127.0.0.1 走回环口绕过防火墙→误以为服务器正常。与证书/掩码无关。
- 修复: `control.py` 新增 `_add_firewall_exception(port)`, 在 `_start_source_server` 启动服务器后调用 `netsh advfirewall firewall add rule name=DiskCopyTool_In_TCP{port} dir=in action=allow protocol=TCP localport={port}` (先 delete 同名旧规则防堆积); 失败区分"拒绝访问(需管理员)"与"其他", 仅告警不阻断启动。WinPE 无 netsh advfirewall 可忽略。
- 同时增强 `_connect_and_download` 的 TCP 探测诊断: 区分 ConnectionRefusedError(端口无监听) / TimeoutError或winerror 10060(防火墙拦截, 提示源端以管理员运行或手动放行 TCP 9999) / winerror 10065等(主机不可达)。
- ⚠️ 关键提示: 防火墙规则添加**需要管理员权限**。若工具以 `--windowed` exe 普通权限运行, `netsh advfirewall` 会"拒绝访问"→规则加不上→仍超时。必须【右键以管理员身份运行】本工具。
- 已知残留环境坑(非阻塞): 接收端 `AddIPAddress` 仍 ret=87 (API 参数无效)、DHCP 服务无法启动。因已统一 /16, 接收端停在 APIPA 也能与源端 /16 直连, 故不影响连通性; 但 DHCP 自动发现源设备流程仍不可用(用户改用手动IP模式连接)。
- 验证: control.py `py_compile` 通过。


- 注：`penetwork_config.py` 仍有 `SUBNET_MASK="255.255.255.0"` 但**全项目未 import 该模块**（grep 0 结果），属死代码，未改。
- `file_transfer.py` 中 /24 字样均为"扫描策略"注释/日志，与掩码配置无关，无需改。
- 验证：四个文件 `py_compile` 通过。



