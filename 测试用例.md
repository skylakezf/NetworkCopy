# 磁盘拷贝工具 — 测试用例（普通用户版）

> 本文档面向**没有任何电脑技术经验**的普通用户。
> 不需要懂 IP、不需要懂网络，只要会"点按钮、看提示、插网线"即可照着做。
> 每一条用例都只描述**你做什么**和**你应该看到什么**。

---

## 一、测试前准备（只需一次）

| 准备项 | 说明 |
|--------|------|
| 两台电脑 | 一台**旧电脑**（数据在它身上，当"发送方"）、一台**新电脑**（准备接收数据，当"接收方"） |
| 一根网线 | 把两台电脑直接连起来（网线插到两台电脑的网口，指示灯亮） |
| 一个 U 盘（可选） | 只在不导出配置的用例中需要用到时准备 |
| 一个测试文件夹 | 在旧电脑 D 盘建一个文件夹，里面放几个小文件和一张照片，方便快速验证 |

> 小提示：为了测试快，可以只在旧电脑 D 盘放一个几 MB 的小文件夹，不要一上来就拷大文件。

---

## 二、用例清单（总览）

| 编号 | 用例名称 | 重要性 |
|------|----------|--------|
| TC-01 | 正常完整流程：导出配置 → 传输 → 导入配置 → 校验 → 完成 | 必测 |
| TC-02 | **不导出配置，直接传输数据** | 必测 |
| TC-03 | **传输中途断网，恢复后继续传输** | 必测 |
| TC-04 | 验证码输错后重新连接 | 必测 |
| TC-05 | 网线没插好 / 网络不通的提示 | 建议 |
| TC-06 | 目标盘已有同名文件（选择保留还是覆盖） | 建议 |
| TC-07 | 接收端不导入配置，直接去校验 | 建议 |
| TC-08 | 校验页直接点"跳过校验" | 建议 |
| TC-09 | 校验时发现缺失文件自动重传 | 建议 |
| TC-10 | 传输完成但校验前直接关掉软件 | 建议 |

---

## 三、详细用例

---

### TC-01 正常完整流程

**场景**：两台电脑第一次完整走一遍，验证软件"能搬完、能校验、能恢复配置"。

**前置条件**：两台电脑已用网线连好；U 盘已插入旧电脑；旧电脑 D 盘有测试文件夹。

| 步骤 | 操作（旧电脑 / 新电脑分别做） | 预期看到的结果 |
|------|------------------------------|----------------|
| 1 | 两台电脑都双击打开「磁盘拷贝工具」 | 两台都出现开始界面，左上角写着"当前角色: 未选择" |
| 2 | **旧电脑**点「旧设备 (发送方)」 | 界面切换，左上角变成"当前角色: 旧设备 (发送方)" |
| 3 | **新电脑**点「新设备 (接收方)」 | 左上角变成"当前角色: 新设备 (接收方)" |
| 4 | **旧电脑**在步骤 1 点「导出系统配置」按钮 | 出现导出进度，最后提示"系统配置已导出到 F:\Appl\日期文件夹"，按钮变成"导出完成"（旧电脑右上会显示导出的项目，如 Outlook/收藏夹/打印机等，没装的项目显示"跳过"是正常的） |
| 5 | **旧电脑**点「下一步 >」进入步骤 2，确认磁盘已自动选中 D/E/F，再点「下一步 >」 | 步骤 2 自动选好了磁盘和盘符映射，可以继续前进 |
| 6 | **旧电脑**到步骤 3 连接页 | 屏幕出现红色大字**4 位验证码**（如 A7K2），并有提示"请在新设备上输入此验证码" |
| 7 | **新电脑**到步骤 3，点「寻找旧电脑」 | 按钮变灰显示"搜索中 (60 秒)..."，等待旧电脑获得 IP |
| 8 | **旧电脑**在连接页点「开始传输」 | 进入传输页，显示验证码横幅和"传输中..."状态 |
| 9 | **新电脑**在验证码框里输入旧电脑屏幕上的**4 位验证码**，点「开始接收」 | 传输开始，进度条和"正在拷贝 X 盘"字样出现，旧电脑日志显示发送中 |
| 10 | 等待传输完成 | 两台电脑都显示"传输成功"，新电脑出现提示框"发现系统配置，是否使用？" |
| 11 | **新电脑**点「使用此配置」 | 自动进入步骤 5 导入配置页并自动开始导入，导入完成后自动跳到步骤 6 校验页 |
| 12 | **新电脑**在步骤 6 点「开始校验」 | 校验进度条滚动，日志逐条显示校验结果，最后显示"全部一致/校验通过" |
| 13 | **新电脑**点「下一步 >」（此时文字为"跳过校验 >"，但校验已完成，实际是进入收尾） | 流程结束，软件提示完成 |

**通过标准**：旧电脑 D 盘的测试文件夹完整出现在新电脑对应盘；校验结果全部"Y"；新电脑恢复了旧电脑的部分配置（如收藏夹）。

---

### TC-02 不导出配置，直接传输数据（重点）

**场景**：旧电脑**不想/忘记**导出系统配置，只想快点把数据搬过去。验证：不导出配置**不影响**数据传输。

**前置条件**：两台电脑已用网线连好；旧电脑 D 盘有测试文件夹；**故意不点导出按钮**。

| 步骤 | 操作 | 预期看到的结果 |
|------|------|----------------|
| 1 | 两台电脑都打开软件，分别选好"发送方 / 接收方" | 同 TC-01 步骤 1-3 |
| 2 | **旧电脑**在步骤 1，**不点**「导出系统配置」，直接点「下一步 >」 | 弹出提示框："未导出系统配置！若不导出，新设备将无法恢复旧设备的系统配置。是否仍要继续？" |
| 3 | 在弹窗里点「是」 | 正常进入下一步，**没有被卡住** |
| 4 | 继续步骤 2、3：确认磁盘、点「开始传输」 | 出现 4 位验证码 |
| 5 | **新电脑**点「寻找旧电脑」→ 输入验证码 → 点「开始接收」 | 传输照常进行，和 TC-01 一样能传完 |
| 6 | 传输完成后观察新电脑 | 新电脑**不会**出现"发现系统配置"提示；进入步骤 5 导入配置页时显示"未检测到配置备份文件夹" |
| 7 | **新电脑**在导入配置页点「跳过」 | 正常进入步骤 6 校验页 |
| 8 | 点「开始校验」，等待完成 | 校验正常通过，数据完整 |

**通过标准**：
1. 弹窗点「是」后能继续，点「否」则停留在原页面（可以在步骤 3 顺带验证"否"的效果）；
2. **数据能正常传输和校验**，一个不少；
3. 唯一区别：新电脑无法恢复配置（导入页提示未检测到配置），这是**预期行为**，不是故障。

---

### TC-03 传输中途断网，恢复后继续传输（重点）

**场景**：传输传到一半，网线松了/被碰掉了。验证：软件会立刻提醒，插回网线后**不用重新开始**，接着没传完的继续传。

**前置条件**：按 TC-01 正常开始传输，并保证数据量稍大一点（例如旧电脑 D 盘放一个 500MB~1GB 的文件或文件夹），让传输有时间被中断。

| 步骤 | 操作 | 预期看到的结果 |
|------|------|----------------|
| 1 | 正常开始传输，让进度走到 30%~50% | 进度条正常前进，状态显示"正在拷贝 X 盘" |
| 2 | **在传输过程中**拔掉网线（或把网线从新电脑上拔出） | 大约几秒后，新电脑传输页出现**红色大字提示**："网络连接已断开 — 请检查网线连接后重新开始传输"；状态栏显示"网络已断开"；「开始接收」按钮变成可点的「**重新接收**」；传输停止，不再继续 |
| 3 | 观察旧电脑 | 旧电脑日志显示连接中断/停止发送，进度不再增长 |
| 4 | 把网线重新插好（确认指示灯亮） | — |
| 5 | **新电脑**点「**重新接收**」按钮 | 重新开始连接；若提示需要重新开启，先点「寻找旧电脑」再点「重新接收」（按界面提示操作即可） |
| 6 | 如果弹出要求输入验证码，输入原验证码；然后等待传输继续 | 传输**从断点接着传**：已经传完的文件会显示"跳过(已存在)"，没传完的文件继续传，**不会从头全部重来** |
| 7 | 等待传输完成并校验 | 全部完成，校验通过 |

**通过标准**：
1. 断网后**几秒内**就有红色提示，不会傻等；
2. 插回网线点「重新接收」后能接上；
3. **已传完的文件被跳过，剩余文件继续传**（断点续传），最终数据完整、校验通过。

> 注意：如果断网发生在传输刚一开始（还没传任何文件），重新接收会从第一个文件开始，这是正常的。

---

### TC-04 验证码输错后重新连接

**场景**：新电脑验证码输错了，软件应该让用户改，而不是卡死或假装成功。

**前置条件**：按 TC-01 走到传输页，但**故意输错**验证码。

| 步骤 | 操作 | 预期看到的结果 |
|------|------|----------------|
| 1 | 新电脑点「寻找旧电脑」，输入一个**错的** 4 位验证码，点「开始接收」 | 传输页出现红色提示"验证码错误 — 请输入正确的验证码后重新连接"；「开始接收」按钮变成「**重试接收**」；上一步按钮文字变成「< 返回修改验证码」 |
| 2 | 点「< 返回修改验证码」回到连接页 | 回到步骤 3，验证码输入框还在 |
| 3 | 重新输入旧电脑屏幕上显示的**正确**验证码，点「重试接收」 | 正常开始传输 |
| 4 | 等待完成 | 传输成功，数据完整 |

**通过标准**：输错验证码有明确红色提示，且能返回修改后重试成功；整个流程不会卡死。

---

### TC-05 网线没插好 / 网络不通的提示

**场景**：两台电脑**没插网线**就点了开始，软件应给出明确提示，而不是一直转圈。

**前置条件**：把网线**拔掉**（或只插一台），直接开始传输。

| 步骤 | 操作 | 预期看到的结果 |
|------|------|----------------|
| 1 | 旧电脑选发送方，点「开始传输」 | 能进入传输页（旧电脑只是开服务，没网线也正常） |
| 2 | 新电脑点「寻找旧电脑」 | 搜索 60 秒后按钮恢复为「寻找旧电脑」，提示未找到/无法获取 IP（也可能提示检查网线） |
| 3 | 新电脑尝试输入验证码点「开始接收」 | 出现提示，说明无法连接到发送端（如"无法连接到源设备"或"连接失败 — 请检查网络"） |
| 4 | **插好网线**，重新按 TC-01 正常走一遍 | 能正常连接和传输 |

**通过标准**：断网/未插线时软件有提示，不会无限转圈；插回网线后一切正常。

---

### TC-06 目标盘已有同名文件（选择保留还是覆盖）

**场景**：新电脑目标盘里已经有同名文件，且大小和旧电脑不一样。软件应弹出选择，让用户决定保留还是覆盖。

**前置条件**：在新电脑的接收盘里**手动创建**一个和旧电脑测试文件夹里同名的文件，但内容改一下（使大小不同）。

| 步骤 | 操作 | 预期看到的结果 |
|------|------|----------------|
| 1 | 按 TC-01 正常开始传输 | 传输到该同名文件时，新电脑弹出**选择窗口**，列出冲突文件，显示两个文件大小，并有绿色「保留已有文件」和红色「覆盖重新下载」两个按钮 |
| 2 | 点「保留已有文件」 | 该文件不覆盖，继续传其他文件 |
| 3 | （如果想验证）再传一次，这次点「覆盖重新下载」 | 该文件被重新下载覆盖，内容与旧电脑一致 |
| 4 | 等待传输完成、校验 | 校验通过 |

**通过标准**：出现冲突选择窗口；两种选择都有效；**不会不打招呼就覆盖**。

---

### TC-07 接收端不导入配置，直接去校验

**场景**：新电脑不想恢复旧电脑的配置（或配置没导），跳过导入，直接校验数据。

**前置条件**：传输已完成（可沿用 TC-01 传输完的状态）。

| 步骤 | 操作 | 预期看到的结果 |
|------|------|----------------|
| 1 | 传输完成后，新电脑进入步骤 5 导入配置页 | 页面显示导入配置选项 |
| 2 | 点「跳过」按钮 | 进入步骤 6 校验页，可以正常点「开始校验」 |
| 3 | 点「开始校验」 | 校验正常进行并完成 |

**通过标准**：跳过导入不报错，校验功能不受影响。

---

### TC-08 校验页直接点"跳过校验"

**场景**：用户不想等校验（或赶时间），直接跳过。验证：软件允许跳过，且会记录"未校验"。

**前置条件**：传输完成，处于步骤 6 校验页（还没有开始校验）。

| 步骤 | 操作 | 预期看到的结果 |
|------|------|----------------|
| 1 | 不点「开始校验」，直接点「下一步 >」（此时按钮文字为「跳过校验 >」） | 软件不报错，流程继续收尾 |
| 2 | 观察日志/提示 | 有跳过校验的记录（软件会生成一份"未校验"的空报告并上传，属于正常行为） |

**通过标准**：能正常跳过校验，不卡死、不报错。

---

### TC-09 校验时发现缺失文件自动重传

**场景**：传输时有极个别文件失败（如文件被占用），校验时软件应自动从旧电脑重传缺失文件，而不是直接判失败。

**前置条件**：最好在旧电脑测试文件夹里放一个**正在被打开**的文档（例如用记事本打开不放），制造一个"占用中"文件，正常传输后该校验可能显示缺失。

| 步骤 | 操作 | 预期看到的结果 |
|------|------|----------------|
| 1 | 按 TC-01 传输并进入校验 | 若有文件在传输时失败，日志会显示该文件"跳过"或"失败" |
| 2 | 点「开始校验」 | 校验日志逐条执行；缺失的文件会自动**重试下载** |
| 3 | 等待校验结束 | 重传成功的文件恢复为"Y"，校验尽量通过；真正无法重传的会标"N"，日志说明原因 |

**通过标准**：缺失文件会尝试自动重传，而不是直接宣告失败；校验结果有明确文字（Y=通过 / N=未通过）。

---

### TC-10 传输完成但校验前直接关掉软件

**场景**：传输完成了，但用户直接关了软件没做校验。验证：软件关闭时不留残留进程，正常退出。

**前置条件**：按 TC-01 传输完成，正处于校验页。

| 步骤 | 操作 | 预期看到的结果 |
|------|------|----------------|
| 1 | 直接点窗口右上角「X」关闭新电脑上的软件 | 软件正常退出，任务管理器里看不到残留的"磁盘拷贝工具"进程（对普通用户：可以再双击打开软件确认能正常启动） |
| 2 | 重新打开软件 | 软件正常启动，没有报错 |

**通过标准**：软件能干净退出，重新打开正常；关闭前软件会尝试发送"未校验"记录（正常行为，不影响使用）。

---

## 四、测试记录表（照着打勾）

| 编号 | 用例名称 | 测试日期 | 结果（通过/失败） | 遇到的问题（记录看到的提示文字） |
|------|----------|----------|--------------------|----------------------------------|
| TC-01 | 正常完整流程 | | | |
| TC-02 | 不导出配置直接传输 | | | |
| TC-03 | 断网后恢复传输 | | | |
| TC-04 | 验证码输错重连 | | | |
| TC-05 | 网线不通提示 | | | |
| TC-06 | 同名文件冲突 | | | |
| TC-07 | 跳过导入配置 | | | |
| TC-08 | 跳过校验 | | | |
| TC-09 | 校验自动重传 | | | |
| TC-10 | 关闭软件退出 | | | |

> 遇到问题时，请把**屏幕上出现的提示文字**原样记下来，并告诉维护人员，这比"不行、坏了"更有帮助。

---

## 五、常见"看着像坏了其实是正常的"情况

| 现象 | 说明 |
|------|------|
| 导出配置时某几项显示"跳过/未配置" | 正常。旧电脑没装 Outlook、没设打印机等，本来就没有这项 |
| 传输时有些文件显示"跳过" | 正常。系统垃圾文件（临时文件、回收站等）程序自动不拷；正在被打开的文件也会跳过 |
| 新电脑导入配置页显示"未检测到配置" | 正常。说明旧电脑没导出配置（见 TC-02），不影响数据 |
| 断网后点「重新接收」提示先开 DHCP | 正常。按提示先点「寻找旧电脑」再重新接收即可 |
| 进度条走到 100% 但日志还在跑校验 | 正常。传输完还要逐个校验，校验会花一些时间 |
