# 磁盘网络拷贝工具 — 程序说明（操作指南底稿）

> 本文档面向 **0 IT 基础用户**，用大白话说明：
> ① 程序会自动"跳过"哪些文件（你不用管）；② 程序有哪几种"工作模式"。
> 所有内容均严格取自程序源码，未做任何功能改动或猜测。
> 用于据此制作一份面向小白用户的操作指南 PPT，请保持口语化、避免术语。

---

## 一、这个程序是干什么的？（一句话）

**不拆硬盘，用一根网线，把老电脑 D/E/F 盘里的东西整盘搬到新电脑。**
搬完还会自动"逐个字核对"，保证一个字节都不差。全程不用你会电脑，按屏幕提示点"下一步"就行。

- 老电脑 = **源设备（发送端）**，插着要搬走的数据盘。
- 新电脑 = **目标设备（接收端）**，准备接收数据。
- 两台用一根网线（或接同一个交换机）连起来，软件自动组网。

---

## 二、程序会自动"跳过"哪些文件？

"跳过"= 程序**主动不拷**某些文件。原因就三类：**没用（垃圾）、系统不让拷、或会搬坏电脑**。
你不需要做任何设置，程序已经替你想好了。下面每一条都对应代码里的实际规则。

### A. 每个盘都会跳过的"系统/垃圾"文件和文件夹
程序在扫描每一个盘（D、E、F…）时，下面的内容会**在任何层级**被自动忽略：

**跳过的文件夹：**
- `AppData` —— Windows 后台缓存（在任何层级都跳过，避免死循环）
- `System Volume Information` —— 系统还原备份区
- `WeChat Files` —— 微信文件（已在其他目录单独处理，避免重复）
- `Application Data` —— 系统交接点，递归会死循环
- 以 `$` 开头的文件夹（如 `$RECYCLE.BIN` 回收站）—— 是"垃圾桶"里的垃圾

**跳过的文件：**
- `ntuser.dat` —— 被系统锁死的注册表文件
- `NTUSER.DAT.LOG1` / `NTUSER.DAT.LOG2` 等（含 `.dat.log`）—— 注册表事务日志
- 以 `~$` 开头的文件（如 `~$报告.docx`）—— Office 自动保存的临时锁文件
- 以 `.tmp`、`.log` 结尾的文件 —— 临时文件 / 事务日志
- 根目录下的：`pagefile.sys`、`hiberfil.sys`、`swapfile.sys`、`DumpStack.log.tmp` —— 虚拟内存/休眠文件，巨大且无用

### B. 根目录下的"程序类"文件夹（默认不拷）
在盘的根目录下，下面这些属于"软件安装目录"，普通搬家用不到，默认跳过：
- `Program Files`、`Program Files (x86)` —— 已装软件
- `ProgramData` —— 软件公共数据

> 说明：这些只在**根目录**层级跳过。如果你的资料夹碰巧叫这个名字（极少见），放在子目录里仍会被正常拷贝。

### C. 其他用户的个人文件夹（只搬"当前登录用户"）
- 如果老电脑有多个用户账号，`Users`（或 `User`）目录里**只搬当前正在用的那个账号**，
  其他人的资料默认不拷（避免把别人电脑的内容也搬过来）。
- 当前用户由程序在扫描时自动识别，无需你选择。

### D. D 盘的"双分区模式"特殊规则（重要）
当老电脑是 **2 个分区**（典型：C 系统盘 + D 数据盘）时，程序对 D 盘做特殊处理：
- **只搬当前用户的 `User/` 个人目录**（桌面、文档、图片、下载等）。
- **同时保留 D 盘根目录下不属于 `User/` 的其他内容**（例如 `GTMC_User_Profiles` 等工作目录）。
- 即：D 盘的"个人文件"精准搬，"非个人根目录"原样保留，互不干扰。

### E. 拷贝过程中"跳不过去"的文件（自动跳过并提示）
- **正在被别的程序打开/占用的文件**（如正在用的数据库、被锁定的程序文件）—— 系统不让读，跳过。
- **没权限读到的文件** —— 跳过。
- 这类文件会出现在结束后的"跳过清单 / 日志"里，方便你事后确认是不是重要东西。

### F. "源盘 = 目标盘"的保护性跳过
- 如果你不小心把**同一个盘**既选成源、又选成目标，程序会跳过并提示"源与目标相同，已跳过"，防止把自己覆盖掉。

### G. 断点续传式的"已存在跳过"
- 如果某个文件在目标盘里**已经存在且大小一致**，程序会跳过（省时间），并在日志写"跳过(已存在)"。
- 若大小对不上，会自动重新下载；开启"覆盖模式"则一律重下。

### H. 导出系统配置时"未配置就跳过"（见第四部分）
- 导出/导入系统设置时，如果老电脑**本来就没设置**某项（如没有 Outlook 规则、没有配置打印机），程序会智能跳过该项并提示"未配置，正常跳过"，不会报错。

---

## 三、程序有哪几种"工作模式"？

### 模式 1：源设备（发送端）—— 老电脑
- 角色：提供数据。
- 干什么：自动列出网线接口准备好的盘 → 你勾选要传的盘（D/E/F）→ 软件自动开"发送服务"等新电脑来取。
- 它会：扫描源盘、把文件切好发出去、记录进度、生成校验码。

### 模式 2：目标设备（接收端）—— 新电脑
- 角色：接收数据。
- 干什么：自动从老电脑"拿"文件，按你指定的盘符落盘，传完自动校验。
- 它会：自动要 IP → 拉文件清单 → 逐盘下载 → 校验指纹 → 生成报告。

### 模式 3：自动组网（无需任何设置）
- 两台一连网线，**不用你设 IP**。新电脑自动拿到 `169.254.100.x` 的局域网地址，老电脑当"服务器"。
- 你只需保证：网线插好、两台都打开本软件、选对角色（谁新谁旧）。

### 模式 4：导出 / 导入系统配置（可选，进阶）
- 用途：把老电脑的**使用习惯**（不是文件，是"设置"）搬到新电脑。
- **发送端步骤 1** 有"导出系统配置"按钮 → 导出到 U 盘/移动硬盘的 `F:\Appl\日期\`。
- **接收端第 5 步"导入配置"** → 选刚才导出的文件夹 → 自动还原：
  - Outlook 邮件账号与规则
  - Chrome / Edge 收藏夹
  - 打印机
  - 输入法（如五笔、拼音词库）
  - IP 网络配置
  - 已安装程序列表（仅记录清单，供你照着装）
- 没有的项目自动跳过，不报错。

### 模式 5：校验模式（自动，看不见但最重要）
- 每个文件传完算一个"指纹"（MD5），接收端再算一遍比对。
- 不一致自动重传；最后生成 `校验报告.csv`，告诉你"全部一致"还是"有几项需关注"。

---

## 四、操作总步骤（界面实际顺序，共 7 步）
> 发送端会自动跳过第 5 步（导入配置仅接收端可见）。

1. **选择网卡 / 导出配置**（发送端在此可导系统配置）
2. **扫描源设备**（列出可传的盘）
3. **选择要传输的盘**（勾选 D/E/F）
4. **传输**（自动拷贝 + 进度）
5. **导入配置**（仅接收端：还原系统设置）
6. **校验**（自动核对指纹）
7. **完成 / 报告**

---

## 五、给用户的"安心三句话"（PPT 收尾用）
1. **跳过的都是垃圾或系统内部文件**，你的照片、文档、微信聊天记录（在 D/E/F 盘的）都会原样搬走。
2. **不用设 IP、不用拆硬盘**，插好网线点"下一步"就完事。
3. **传完会自己核对**，少一个字节它都重传，放心。

---

## 六、跳过项速查表（PPT 图标化呈现）
| 类别 | 典型例子 | 为什么跳过 |
|------|----------|-----------|
| 回收站 | $RECYCLE.BIN | 是垃圾 |
| 系统还原 | System Volume Information | 系统备份 |
| 后台缓存 | AppData | 用不到 |
| 虚内存 | pagefile.sys / hiberfil.sys | 巨大且无用 |
| 临时文件 | *.tmp / ~$*.docx | 临时垃圾 |
| 注册表锁 | ntuser.dat | 系统锁死读不到 |
| 已装软件 | Program Files | 重装更稳 |
| 别人账号 | Users/其他用户 | 不是你的 |
| 占用中 | 打开的数据库 | 系统不让读 |
| 相同盘 | 源=目标 | 防覆盖 |
| 未配置项 | Outlook 无规则 | 本来就没有 |

---

*（本文为操作指南 PPT 底稿，全部"跳过"逻辑与"模式"均严格取自程序源码，未做功能改动或主观猜测。）*
