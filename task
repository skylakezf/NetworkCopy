总概
这是一个在WindowsPE下运行的设备数据拷贝工具，在WindowsPE下将设备DEF盘的数据拷贝至目标设备的目标磁盘中
GUI页面我已经生成使用标准的tkinter，此外文件夹内我已经下载了python嵌入式版本

GUI页面描述

├──────────────────────────────────────────────────────────────┤
│                                                                │
│  ┌──────────┐                              ┌──────────┐       │
│  │ 选择网卡  │                              │ 设备类型  │       │
│  └──────────┘                              └──────────┘       │
│  ┌─────────────────────┐                   ┌──────────┐       │
│  │  网卡1 / 网卡2      │                   │源设备/目标│       │
│  └─────────────────────┘                   └──────────┘       │
│                                                                │
│  ┌──────┐  ┌──────┐                                            │
│  │ 磁盘  │  │ 正确的分区名称D、E、F  │                                           │
│  └──────┘  └──────┘                                            │
│  ┌────────┐ ┌──┐ ┌──┐ ┌──┐                                    │
│  │ 磁盘1/2 │ │ D │ │ E │ │ F │                                  │
│  └────────┘ └──┘ └──┘ └──┘                                    │
│                                                   ┌────────┐  │
│                                                   │开始传输 │  │
│                                                   │ /接收   │  │
│  ┌──────┐                                       └────────┘  │
│  │ log  │                                                    │
│  └──────┘                                                    │
│  ┌──────────────────────────────────────────────────────┐    │
│  │                                                      │    │
│  │              日志输出区域 (Text)                      │    │
│  │                                                      │    │
│  └──────────────────────────────────────────────────────┘    │
│                                                                │
└──────────────────────────────────────────────────────────────┘


规则
初始化程序后将所有能够识别到的网卡输出在网卡下拉列表中
选项格式为 网卡名称 最高支持速率 如
Intel(R) Ethernet Connection I219-V 1Gbps
Intel(R) Ethernet Connection I229-V 2.5Gbps

用户选择设备类型后
源设备
将选择的网卡IP地址设置为10.0.0.1/24
目标设备
将选择的网卡IP地址设置为10.0.0.2/24

在此之后
在磁盘下拉选项框中输出所有磁盘名称格式为
WDC PC SN730 SDBQNTY-512G-1001
WDC PC SN730 SDBQNTY-1024G-1001


操作：
设置源设备
进入winpe 
设备类型选择为源设备
选择网卡后
程序自动帮助我设置IP地址

选择磁盘后由于WindowsPE的分区是错乱的因此需要用户手动映射正确的分区盘符

三个选项框实际表示的是，系统下D、E、E分区对应的Winpe下磁盘盘符

如我选择
I、J、K
则表明
I盘为正常系统下的D盘
J盘为正常系统下的E盘
K盘为正常系统下的F盘

完成后设置目标设备

进入winpe 
设备类型选择为目标设备
选择网卡后
程序自动帮助我设置IP地址

选择磁盘后由于WindowsPE的分区是错乱的因此需要用户手动映射正确的分区盘符

三个选项框实际表示的是，系统下D、E、E分区对应的Winpe下磁盘盘符

如我选择
I、J、K
则表明
I盘为正常系统下的D盘
J盘为正常系统下的E盘
K盘为正常系统下的F盘


从原设备上点击开始按钮向目标设备传输磁盘DEF的数据
日志输出区域输出所有日志

目的
通过网络传输设备的所有数据，保证磁盘数据的正确分布
如源设备D盘的数据必须位于目标设备D盘中不得存在错乱
传输完成后打开磁盘F\Appl\文件夹下遍历所有文件夹，文件夹是以YYYY/MM/DD命名的，进入最新的文件夹也就是YYYY/MM/DD最大的文件夹
打开FullFilelist_DEF.csv文件文件内B列FullPath为所有文件的路径，你需要在目标设备上完成校验若校验完成，你需要在E列填入Y否则填入N



========== 以下为技术分析 ==========

一、难点与卡点分析

1. Windows PE 环境限制
   - WMI 不可用，wmic/Win32_NetworkAdapter 等无法使用
   - 嵌入式 Python 无 pip，依赖需手动管理
   - netsh 可能不完整
   对策：优先纯 Python 标准库 + ctypes 调用 Windows API + 备选命令行

2. 网卡枚举与速率获取
   - 需获取网卡名称和最高支持速率
   - 格式如：Intel(R) Ethernet Connection I219-V 1Gbps
   对策：ctypes 调用 iphlpapi.dll 的 GetAdaptersAddresses，备选解析注册表

3. IP 地址自动配置
   - 源设备设为 10.0.0.1/24，目标设备设为 10.0.0.2/24
   对策：优先 subprocess 调 netsh，fallback ctypes 调 IP Helper API

4. 物理磁盘枚举
   - PE 无 WMI，需获取磁盘型号（如 WDC PC SN730...）
   对策：CreateFile(\\.\PhysicalDriveX) + DeviceIoControl(IOCTL_STORAGE_QUERY_PROPERTY)

5. 分区盘符映射（核心难点）
   - PE 下盘符与正常系统完全不一致
   - 用户需手动将 PE 盘符映射到正常系统 D/E/F 盘
   对策：GetLogicalDrives 枚举 PE 可用盘符填入 Combobox

6. 高效网络数据传输
   - 大量/大文件需保持目录结构
   对策：源端启动简易 HTTP 文件服务器，目标端 HTTP GET 逐文件下载

7. CSV 校验
   - 定位 F:\Appl\ 下最新 YYYY/MM/DD 文件夹
   - 解析 FullFilelist_DEF.csv，B列路径校验，E列填入 Y/N
   对策：csv + hashlib.md5，datetime 比较日期

二、推荐技术栈

   GUI：       Tkinter + ttk（标准库，已生成）
   网卡枚举：  ctypes + iphlpapi.dll
   IP 配置：   subprocess(netsh) → fallback ctypes API
   磁盘枚举：  ctypes + kernel32.dll / DeviceIoControl
   网络传输：  http.server(源端) + urllib.request(目标端)（标准库）
   文件校验：  hashlib.md5 + csv（标准库）
   并发：      threading（不阻塞 GUI）

三、实现路线

   Phase 1: 网卡扫描 & IP 配置
     1.1 枚举物理网卡（名称+速率）
     1.2 填充网卡下拉列表
     1.3 根据设备类型自动设置静态IP
   Phase 2: 磁盘扫描 & 盘符映射
     2.1 枚举物理磁盘（型号名称）
     2.2 枚举 PE 下可用盘符
     2.3 用户选择分区映射
   Phase 3: 网络数据传输
     3.1 源端启动 HTTP Server
     3.2 目标端下载 D/E/F 数据
     3.3 保持目录结构 1:1
     3.4 GUI 日志实时输出
   Phase 4: 数据校验
     4.1 定位 F:\Appl\ 最新日期文件夹
     4.2 解析 FullFilelist_DEF.csv
     4.3 逐文件 MD5 校验
     4.4 E列写入 Y/N
   Phase 5: 整合 & PE 实际测试

四、代码架构

   main.py          → 入口
   ui.py            → 纯布局（不变）
   control.py       → 业务逻辑层
     ├── nic_scanner.py    → 网卡扫描
     ├── ip_config.py      → IP 配置
     ├── disk_scanner.py   → 磁盘枚举
     ├── file_transfer.py  → 文件传输
     ├── verifier.py       → CSV 校验
     └── logger.py         → 日志管理

